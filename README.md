
  # Build Frontend

  This is a code bundle for Build Frontend. The original project is available at https://www.figma.com/design/i91nVDQlthp8p5E6miwey6/Build-Frontend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  